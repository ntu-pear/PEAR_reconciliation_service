# Service URLs (use localhost for local dev, k8s service names in cluster)
ACTIVITY_SERVICE_URL=http://localhost:8080/api/v1
PATIENT_SERVICE_URL=http://localhost:8000/api/v1
SCHEDULER_SERVICE_URL=http://localhost:3000

# RabbitMQ Configuration
RABBITMQ_URL=amqp://admin:pear2025@127.0.0.1:5672/vhost

# Reconciliation Schedule (cron format: minute hour day month day_of_week)
# Default: "55 * * * *" (every hour at minute 55)
RECONCILIATION_SCHEDULE=55 * * * *

# Logging Configuration
LOG_LEVEL=INFO

# Service Configuration
SERVICE_NAME=reconciliation-service
PORT=8000
